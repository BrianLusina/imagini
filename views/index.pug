- var titleContent = 'Imagini, an Image Search Abstraction Layer'; 

doctype html
html
  head
    title #{titleContent}
    link(rel='stylesheet' href='https://maxcdn.bootstrapcdn.com/bootstrap/3.3.6/css/bootstrap.min.css')
    link(rel="stylesheet" href="/style.css")
  body
    div.container
        h1.header #{titleContent}
        blockquote
            | User stories:
            ul 1) I can get the image URLs, alt text and page urls for a set of images relating to a given search string.
            ul 2) I can paginate through the responses by adding a ?offset=2 parameter to the URL.
            ul 3) I can get a list of the most recently submitted search strings.
        h3 Example usage:
        code
            a(href='https://hypnotic-drink.glitch.me/api/images/search/lolcats%20funny?offset=1' target='_BLANK') https://hypnotic-drink.glitch.me/api/images/search/lolcats%20funny?offset=1
        br
        code
            a(href='https://hypnotic-drink.glitch.me/api/images/latest' target='_BLANK') https://hypnotic-drink.glitch.me/api/images/latest